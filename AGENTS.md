# 项目开发规则

## 最小改动与验证

- 开始任务前只读取完成当前任务所必需的相关代码、配置和测试；仅在需要历史决策、先前实验结果或上下文时读取工作日志相关记录。
- 在现有项目上增量修改，只处理本轮目标，不为记录日志改变生产逻辑。
- 测试范围与改动风险匹配。优先运行直接相关测试；跨模块、关键路由、SSE、计费、上下文保护等高风险变更时再扩大到完整回归。已有且仍适用的验证结果可复用。完成后核验真实结果。
- 不把 mock / dry-run 的输出当作真实模型质量、实际用量或费用证明。
- 删除文件、大范围破坏性修改、付费操作须事先取得用户确认。已有授权仅适用于其明确范围；历史 live 记录不授权新一轮付费调用。
- 不硬编码密钥，不提交 `.env`、本地凭据或私人数据。

## 工作日志的触发范围与追加规则

仅对产生值得追踪的项目变更、架构或技术决策、实验结果、失败与修复、里程碑，或用户明确要求记录的任务，在 [docs/project-worklog.md](docs/project-worklog.md) **末尾追加**工作日志。纯问答和只读审计默认不写。小改动可简要记录，重要开发阶段使用完整模板。

满足上述记录条件且本轮未明确排除日志更新时，执行顺序为：

1. 完成代码、配置或文档修改。
2. 按上述风险范围执行必要测试或复用仍适用的验证结果；未执行时如实说明，不能声称本轮已执行通过。
3. 验证结果、改动范围及敏感信息边界。
4. **最后更新 `docs/project-worklog.md`**，随后可以只读核验写入完整性。

写日志前先检查文件当前末尾，防止并行任务覆盖他人的记录。保留全部历史内容，不覆盖、不删除、不重新排序旧记录。发现旧记录有误时，追加“更正/补充”，引用原记录编号并说明证据。若误录敏感内容，停止传播并按用户授权安全处理，不能用追加式规则为保留秘密辩护。

每条记录使用唯一顺序编号；小改动可简要记录，重要开发阶段使用以下完整模板。缺少证据的字段写“待补录”；不适用的字段可省略。

```markdown
## WXXX — 本轮任务名称

- 日期/时间：YYYY-MM-DD HH:mm:ss +08:00；历史补录另列事件时间与补录时间。
- 本轮目标：
- 用户本轮关键提示词（原文，脱敏）：
  > 保留关键句；长提示词可以另附摘要，但不能只有摘要。
- 对提示词的理解：
- 技术决策 / Decision：选择、理由和取舍；没有新架构决策时明确说明。
- 新增/修改的文件：使用项目相对路径；没有修改时写明。
- 主要修改的原因：对应每个主要文件或同一目的的文件组。
- 执行过的测试：命令、模式和范围；区分本轮执行与引用历史结果。
- 测试结果：实际数字、失败项及证据位置，标明 actual / estimated / unknown / simulated。
- 错误/失败 / Failure / Lesson：保留失败实验、报错、回滚和踩坑，不只记录成功。
- 错误原因：区分已证实原因、推测和待确认内容。
- 如何解决：实际采取的措施及验证；未解决时明确写出。
- 仍未解决的问题：
- 下一步建议：仅在存在未解决问题或用户明确要求时填写；建议不等于已经执行或获得付费授权。
- 证据：报告、命令结果或脱敏对话出处；无法确认的细节写“待补录”。
```

重要架构取舍必须标记 **Decision**，包括路由方式、本地与云端分工、must_keep 保护、延期的复杂能力及原因。失败或教训必须标记 **Failure / Lesson**，包括端口冲突、凭据配置、编码错误、上下文误删、窗口误裁剪、用量与延迟取舍、材料完整但回答错误等实际发生的情况。

## 隐私与事实边界

- 禁止写入 API Key、认证/访问 Token、其他密钥、密码、私人代码、真实工作数据或隐私内容；不复制环境变量值、完整服务日志、数据库快照或含敏感内容的对话。
- 模型输入/输出 token **数量**仅作为无敏感内容的实验指标记录，与认证 Token 区分；不能为复现实验记录凭据。
- 提示词先脱敏，移除私人路径、账号、真实业务素材。保留关键原文，用 `[已脱敏]` 表示删去的敏感部分；优先使用相对路径。
- 只记录有证据支持的事实；区分“计划”“用户报告”“命令/报告验证”。对话时间不冒充命令完成时间，第一次可追溯记录不冒充绝对首次执行。
- 保留模拟/真实、估算/供应商用量、费用未知/零付费调用之间的区别；不合并不同阶段的测试结果。
- 默认离线。除非用户明确授权本轮付费测试，不执行 `--live` 或真实云请求。日志任务本身不需要启动模型。

## 项目完成和开源前

`docs/project-worklog.md` 是持续追加的原始开发时间线。只有项目最终完成时，才基于它整理正式的 `WORKLOG.md`、`PROMPTS.md`、`DECISIONS.md`、`FAILURES.md`、`LESSONS.md`；不要提前生成或用正式叙事替代原日志。

最终准备上传 GitHub 前，另外执行开源清洗：检查敏感信息、真实密钥及需要去除的本地隐私路径；保留可复现实验数据、脱敏关键提示词、失败实验和架构取舍。清洗与上传是独立步骤，不能把建立本规则视为已经清洗或获得发布授权。
